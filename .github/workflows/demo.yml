name: Demo Workflow
on:
  pull_request:
    branches: [ main ]
  push:
    branches: [ bug-bounty-poc ]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Use local setup action
        uses: ./.github/actions/setup
      - name: Echo success
        run: echo "Workflow executed successfully"